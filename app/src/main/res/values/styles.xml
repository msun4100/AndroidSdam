<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
	<!--           
         <item name="actionBarStyle">@style/CustomActionBarStyle</item>
      -->    
      <item name="actionBarSize">@dimen/z_actionbar_height</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        
        <!-- 
        <item name="actionBarStyle">@style/CustomActionBarStyle</item>
        <item name="android:windowContentOverlay">@null</item>
         -->
         <!-- 
        <item name="actionBarStyle">@style/tamnaTheme</item> 
        <item name="searchViewStyle">@style/MySearchView</item>
          -->

        <item name="colorPrimary">@color/colorPrimary</item>
        <!--<item name="colorPrimaryDark">@color/colorPrimaryDark</item>-->
        <item name="colorAccent">@color/colorAccent</item>


        <item name="actionBarSize">@dimen/z_actionbar_height</item>
        
    </style>
    <style name="CustomActionBarStyle" parent="@style/Widget.AppCompat.ActionBar">
        <item name = "android:background">@drawable/b_main_view_1_actionbar_logo</item>
        <item name= "background">@drawable/b_main_view_1_actionbar_logo</item>
    </style>
    <style name="MyDialog" parent="android:Theme.Dialog">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@drawable/dialog_background</item>
    </style>
        
</resources>
